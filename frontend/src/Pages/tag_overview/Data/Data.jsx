const data =[
    {
        id:"1",
        question:"Why is processing a sorted array faster than processing an unsorted array? ",
        description:"Here is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times faster Here is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times fasterHere is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times fasterHere is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times fasterHere is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times faster:",
        tag:{"first":"python", "second":"java"}
    },
    {
        id:"2",
        question:"whi",
        description:"Here is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times faster:"
    },
    {
        id:"3",
        question:"whi",
        description:"Here is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times faster:"
    },
    {
        id:"4",
        question:"whi",
        description:"Here is a piece of C++ code that shows some very peculiar behavior. For some strange reason, sorting the data miraculously makes the code almost six times faster:"
    },
    {
        id:"5",
        question:"whi",
        description:" iraculously makes the code almost six times faster:"
    },

]

export default data;